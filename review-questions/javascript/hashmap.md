# What does it mean to hash?
Takes an input and generates a corresponding output depending on the data to be stored.
# What are buckets?
Stores data according to a hash code
# What is a collision?
When two or more pieces of data are stored in a hash bucket
# When is it a good time to grow our buckets array?
When the number of items are greater than capacity * load factor
