# What is npm?
A package manager- a large repository that provides us with a command line tool to install packages.
# What file does npm use that contains all information about dependencies?
package.json