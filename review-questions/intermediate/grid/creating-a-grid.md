# How does a HTML element become a grid item?
display: grid;
# What is the space between lines on a grid?
gap
# How do you set gutters/ alleys on a grid?
column-gap and row-gap
# Define what happens when you have more content than defined tracks?
The rows would autofill.
# How can you change the size of those undefined tracks?
grid-auto-rows and grid-auto-columns