# What is the difference between static and relative positioning?
Static positioning positions the element in its default place while relative positioning has an offset.
# What is absolute positioning useful for?
Keeping an element in an exact location on a webpage.
# What is the difference between fixed and sticky positioning?
Sticky positioning stops at the edge of the screen while fixed positioning stays in a fixed location on the page.